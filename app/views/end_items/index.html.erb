<% if end_user_signed_in? %>
	<%= render "end_header/login_header" %>
<% else %>
	<%= render "end_header/not_login_header" %>
<% end %>

<div class="container">
	<div class="row">
		<div class="col-xs-9">
			<div class="row">
				<%= render :partial => "new_items" %>
				<%= render :partial => "cheap_items" %>
				<%= render :partial => "few_items" %>
				<%= render :partial => "all_items" %>
			</div>
		</div>
		<%= render "ranking" %>
	</div>
</div>
